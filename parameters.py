# Telegram settings
# Your Telegram username (messages will be sent to this user)
username = ''
api_id = ''         # Telegram bot API ID (usually an 8-digit number)
api_hash = ''       # Telegram bot API hash
bot_token = ''      # Telegram bot token
session_name = ''   # A unique name to create the virtual telegram client

# general settings
# recommended to make it > 10 seconds to account for processing time, make it > 30 when using random proxies
polling_interval_seconds = 60
report_after_n_counts = 60
config_path = 'config.json'
data_path = 'data.csv'
log_path = 'log.txt'
randomize_proxies = False
